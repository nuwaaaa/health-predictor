# Health Predictor 改善プラン

前回のレビューで挙げた弱みを具体的に整理し、修正の優先度と方針をまとめる。

---

## 1. 欠損値処理の改善
**現状:** `features.py:117-123` — 3段階補完（7日rolling mean → 全体mean → 0）
**問題:** 時系列の文脈を十分活かせていない。特に最終フォールバックの `0` は意味的に不適切な場合がある（例: sleep_hours=0）。
**改善案:**
- 最終フォールバックを `0` ではなく特徴量ごとの妥当なデフォルト値に変更
- 例: sleep_hours → 7.0, steps → 5000, stress → 3.0 など
**工数:** 小（config.pyに定数定義 + features.pyの`_fill_missing`修正）
**リスク:** 低
**優先度:** ★★★（高）

---

## 2. Calibration（確率の較正）検証の追加
**現状:** `models.py` — PR-AUC / ROC-AUCのみ。予測確率の較正チェックなし。
**問題:** ユーザーに確率を表示する場合、予測値と実際の発生率が乖離していると誤解を招く。
**改善案:**
- Brier scoreをCV評価に追加
- 評価結果に `brier_score` を含めてログ出力
- 必要に応じてPlatt scaling / isotonic regressionを検討（ただしデータ量が少ないため慎重に）
**工数:** 中（models.pyの評価ループに指標追加 + テスト更新）
**リスク:** 低（既存の動作には影響しない追加指標）
**優先度:** ★★★（高）

---

## 3. 特徴量の交互作用項の追加
**現状:** `features.py:79-99` — 全て1次特徴量のみ。交互作用なし。
**問題:** LRでは睡眠×ストレスなどの非線形パターンを捉えられない（LightGBMは木で暗黙的に扱える）。
**改善案:**
- `sleep_hours_filled * stress_filled`（睡眠不足＋高ストレスの複合効果）
- `steps_filled * stress_filled`（運動＋ストレスの相互作用）
- LRモデル使用時のみ効果的。LightGBM使用時はツリーが自動的に扱うため不要。
**工数:** 小（features.pyに2-3行追加）
**リスク:** 中（過学習の可能性。CV指標で検証必要）
**優先度:** ★★（中）

---

## 4. 周期特徴の高次高調波（2nd harmonic）追加
**現状:** `features.py:22-27` — day_sin/day_cosは1次のみ（`2π * dow / 7`）。
**問題:** 週の中で2峰パターン（例: 月曜と金曜が悪い）を捉えられない。
**改善案:**
- 2次高調波を追加: `sin(4π * dow / 7)`, `cos(4π * dow / 7)`
- bed/wakeは1次で十分（連続値のため）
**工数:** 小（features.pyに2行追加 + モデルの特徴量リスト更新）
**リスク:** 中（特徴量増加による過学習。データ60日未満では逆効果の可能性）
**優先度:** ★（低） — データ量が個人単位で少ないため、効果は限定的

---

## 5. アドバイスとモデルの一貫性確保
**現状:** `advice.py` — モデル予測とは独立した統計集計でアドバイス生成。`pipeline.py:185-189` で `p_today` を渡しているが使われていない。
**問題:** モデルが「睡眠は重要でない」と判断してもアドバイスは「もっと寝ましょう」と言う可能性がある。
**改善案:**
- モデルの特徴量寄与度（SHAP / LR係数）をアドバイスの閾値調整に活用
- 例: SHAPで寄与が低い特徴に関するアドバイスを抑制 or 優先度を下げる
**工数:** 大（advice.pyの設計変更 + contributions情報の受け渡し）
**リスク:** 高（モデル精度が低い初期段階では逆に有害なアドバイス抑制になりうる）
**優先度:** ★（低） — 設計上、統計的に有意な差がある場合のみアドバイスを出す現行方式も合理的

---

## 修正優先順位まとめ

| # | 改善項目 | 優先度 | 工数 | リスク |
|---|---------|--------|------|--------|
| 1 | 欠損値フォールバック改善 | ★★★ | 小 | 低 |
| 2 | Brier score追加 | ★★★ | 中 | 低 |
| 3 | 交互作用項追加 | ★★ | 小 | 中 |
| 4 | 2次高調波追加 | ★ | 小 | 中 |
| 5 | アドバイス×モデル一貫性 | ★ | 大 | 高 |

**推奨:** #1と#2は低リスクで効果が高いためすぐ着手。#3はCV結果を見て判断。#4と#5はデータ量が増えてから検討。
